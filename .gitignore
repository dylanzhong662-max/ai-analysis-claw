# 环境变量（含 API Key，禁止提交）
.env
.env.local

# Python 缓存
__pycache__/
*.pyc
*.pyo

# ── 运行时输出文件（每日自动生成，不需要提交）──
*_api_output.txt
*_prompt_output.txt

# 市场扫描输出
market_scan_output.json
market_scan_report.txt
market_scan_prompt.txt

# 持仓 / 订单状态
portfolio_status.json
overlay_status.json
orders.json

# 信号日志
live_signal_log.csv
ic_analysis_results.csv

# 数据库
trading.db

# ── 数据缓存（可本地重新生成）──
data_cache/

# ── 运行日志 ──
logs/
nohup.out

# ── 服务器日志镜像 ──
logs_server/

# ── macOS 系统文件 ──
.DS_Store
**/.DS_Store

# ── 前端构建产物 ──
frontend/node_modules/
frontend/dist/
